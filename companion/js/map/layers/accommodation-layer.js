/*
 * Capa ACCOMMODATION — marcador del alojamiento de la etapa activa.
 * No depende de POIs ni de la ruta; solo pinta el punto que ya viene
 * resuelto en los datos de la etapa. Implementación real en Fase 5.
 */
export function createAccommodationLayer(mapEngine) {
  return {
    setAccommodation(_accommodation /* { name, lat, lng } */) {
      /* Fase 5 */
    },
    clear() {
      /* Fase 5 */
    },
  };
}
